const express = require('express');
const jwt = require('./utils/jwt');

/** initial config ------------------------------------------------------------------------- */
const app = express();
require('dotenv').config();

/** routes -------------------------------------------------------------------------------- */

/** server -------------------------------------------------------------------------------- */
