import React from 'react';

import Todo from './Todo'

const TodoList = props => {
    return (
        <div>
            TodoList
            <Todo />
        </div>
    );
}

export default TodoList;